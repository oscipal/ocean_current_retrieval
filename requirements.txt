# ======================================================================
# Python dependencies for the Sentinel-1 ocean-current retrieval.
# Conda env name used in development: "ocean".
#
#   conda create -n ocean python=3.12
#   conda activate ocean
#   pip install -r requirements.txt
#
# NOTE: the GAMMA SAR processor is an external commercial package and is
# NOT installable via pip. It must be installed separately and its bin/
# directory placed on PATH (used for the Doppler estimation).
# ======================================================================

# --- core numerics / plotting ---
numpy==2.4.2
scipy==1.17.0
pandas==3.0.0
matplotlib==3.10.8

# --- geospatial / NetCDF I/O ---
xarray==2025.12.0
netCDF4==1.7.4
h5netcdf==1.8.1
rasterio==1.4.4
shapely==2.1.2
pyproj==3.6.1

# --- data download (ERA5, CMEMS GLO12 / drifters) ---
cdsapi==0.7.7
copernicusmarine==2.4.1
requests==2.33.1
